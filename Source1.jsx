clip = app.project.activeSequence.audioTracks[3].clips[1];//~ //fadeAudio(0.3, clip);volume = clip.components[0].properties[1];$.writeln(clip.name);$.writeln(volume.displayName);$.writeln(volume.getValue());fadeAudio(0.05, clip);function fadeAudio(length, audioClip) {    //get length of audioClip    clipLength = audioClip.outPoint.seconds - audioClip.inPoint.seconds;    param = audioClip.components[0].properties[1];    // allow keyframes    param.setTimeVarying(true);    // add keyframes at beginning and end of the image layer (for opacity)    param.addKey(audioClip.inPoint.seconds);    param.addKey(audioClip.inPoint.seconds+length);    param.addKey(audioClip.inPoint.seconds+clipLength-length);    param.addKey(audioClip.inPoint.seconds+clipLength);        // change the keyframes to be 0 and 100    param.setValueAtKey(audioClip.inPoint.seconds, 0);    param.setValueAtKey(audioClip.inPoint.seconds+length, 100);    param.setValueAtKey(audioClip.inPoint.seconds+clipLength-length, 100);    param.setValueAtKey(audioClip.inPoint.seconds+clipLength, 0);    }